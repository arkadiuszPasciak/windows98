import { JSX } from 'react';
import { DSButtonProps } from './ds-button.type';
export declare const DSButton: ({ className, size, disabled, onClick, text, id, isTextVisible, iconLeft, iconRight, }: DSButtonProps) => JSX.Element;
