# [lint-staged](https://github.com/lint-staged/lint-staged)

Lint-staged configuration for Windows 98 project

## CLI

### lint:staged

Command is used to run lint-staged on your files in order to `check` for code style and syntax issues.

```bash
pnpm --filter @windows98/lint-staged run lint:staged
```
