import type { ProgramManager } from "../types"

export const programManagerConfig: ProgramManager = {
	calculator: false,
	notepad: false,
	run: false,
	settings: false,
	shutDown: false,
	ticTacToe: false,
	timer: false,
}
