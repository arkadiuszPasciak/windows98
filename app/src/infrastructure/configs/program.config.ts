import type { ProgramManager } from "../types"

export const programManagerConfig: ProgramManager = {
	calculator: false,
	notepad: false,
	runner: false,
	settings: false,
	shutdown: false,
	"tic-tac-toe": false,
	timer: false,
}
