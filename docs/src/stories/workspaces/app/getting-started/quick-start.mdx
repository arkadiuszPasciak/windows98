import { Meta, Title } from "@storybook/addon-docs/blocks";

<Meta title="Workspaces/App/Getting Started/Quick Start" />

<Title>🏎️ Quick Start</Title>

Follow these steps to set up and run the Windows98 app in your development environment.

1. **Install Dependencies**
   ```bash
   pnpm install
   ```

2. **Navigate to App Workspace**
   ```bash
   cd app
   ```

3. **Start Development Server**
   ```bash
   pnpm dev
   ```

4. **Open in Browser**
   The app will be available at `http://localhost:5173` (or the port shown in terminal)
