import { Meta, Title } from "@storybook/addon-docs/blocks";

<Meta title="Micro Services/Services/MSAppConfig" />

<Title>MSAppConfig</Title>

The `ms-app-config` microservice manages the application configuration, ensuring all required settings are properly set and can be retrieved or updated as needed.