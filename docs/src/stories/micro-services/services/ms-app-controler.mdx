import { Meta, Title } from "@storybook/addon-docs/blocks";

<Meta title="Micro Services/Services/MSAppControler" />

<Title>MSAppControler</Title>

The `ms-app-controller` microservice handles application control operations such as restarting and shutting down the application, ensuring smooth and controlled transitions.